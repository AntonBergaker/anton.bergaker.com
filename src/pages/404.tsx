import * as React from 'react';
import { Link } from 'gatsby';
import { yourMom } from './404.module.scss';

// markup
const NotFoundPage = () => {
    return <p className={yourMom}>p</p>;
};

export default NotFoundPage;
