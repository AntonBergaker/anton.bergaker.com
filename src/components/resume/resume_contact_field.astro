---
import { contactField, contactFieldImage } from "./index.module.scss";

type Props = {
    text: string;
    link?: string;
};

const props = Astro.props;
---
<div class={contactField}>
    <div class={contactFieldImage}><slot/></div>
    <a href={props.link ?? props.text}>{props.text}</a>
</div>